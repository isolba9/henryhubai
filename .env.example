# All API keys are entered by users via the interface.
# These environment variables serve as optional server-side defaults.
# If set, they will be used when no user-provided key is available.

ANTHROPIC_API_KEY=
API_NINJAS_KEY=
EIA_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_ANON_KEY=
